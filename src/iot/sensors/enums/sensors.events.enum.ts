export enum SensorsIncomingEvents {
  SYSTEM_REGISTERED_SENSORS = 'system-registered-sensors',
}

/**
 * This enum contains the events that are sent to the client
 * related to the system sensors in general.
 */
export enum SensorsOutgoingEvents {
  SYSTEM_REGISTERED_SENSORS = 'system-registered-sensors',
}
